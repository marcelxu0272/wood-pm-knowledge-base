# 📊 项目追踪表 — 项目执行跟踪与最新动态

> **最后更新：** 2026-05-21 10:50
> **当前阶段：** 原型开发与核心功能实现（前端填报/审批/看板基本成型，需求文档持续迭代）
> **跟踪范围：** PM 知识库管理的所有项目及重点任务
> **项目文件夹路径：** C:\Work\1_Projects\202605_项目追踪表线上化

---

## 🎯 核心项目概览

| 项目名称 | 负责人 | 当前状态 | 最新进展 | 下次里程碑 | 风险提示 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 项目追踪表线上化 | Ethan, 比比 | 前端原型开发中 | 填报页（Luckysheet）、分板块审批流、PM 提交/板块接收、存量指标 R/S 预警、平台数据同步框架、项目详情 Drawer（P0/P1）、系统管理员底栏、Diff 高亮与批注等已实现；需求文档 v2.17+（753 行），数据库设计 9 张表完成 | 平台数据同步 API 对接、12 板块全量注册、前端性能优化 | 财务系统接口对接复杂度、多板块数据权限隔离、大数据量下 Luckysheet 性能 |

---

## 📝 详细进度追踪

### 1. 产值报告线上化项目
- **项目背景：** 将 S520 及全公司项目执行跟踪 Excel 表线上化，实现多角色协作填报与审批。
- **当前阶段：** 前端原型核心功能已基本成型，需求文档与数据库设计持续迭代。
- **关键成果：**
  - **需求文档：** `线上化需求.md` 迭代至 v2.17+（753 行），涵盖 6 角色定义、双轨填报、锁定期、变更高亮、数据初始化、分板块审批、PM 提交/板块接收、存量 R/S 预警与完成额校验、平台数据批量同步、7 项补充需求（§2.16）、6×83 字段权限矩阵（§2.17）。
  - **数据库设计：** `database-schema.sql` + `database-schema-design.md`，9 张表（项目主表、月度填报、年度基线、预收款核销、审计日志、审批快照、PM 提交、用户角色、系统配置）。
  - **技术栈：** 基于 HTML5/JS 轻量级架构 (No Build Tools)，Vue2 + ElementUI + Luckysheet + ECharts + TailwindCSS + SQLite。
  - **前端核心模块（24+ JS 文件）：**
    - **填报表格：** `ProjectEditor.js`（Luckysheet 填报页，含权限控制/筛选/高亮/公式联动）
    - **项目详情 Drawer：** `ProjectDetailDrawer.js`（点击项目号打开的填报面板，P0/P1）
    - **审批流程：** `Approval.js`、`ApprovalReviewSheet.js`（只读 Luckysheet + 三态时间轴 + 通过/驳回）
    - **系统管理：** `AdminSettings.js`（管理设置页）、`SystemAdminSectorDock.js`（底栏 12 板块监控）、`SystemAdminApprovalBoard.js`（审批看板）
    - **数据看板：** `Dashboard.js`（ECharts 可视化）
    - **审计日志：** `AuditLog.js`（变更日志查询）
    - **存量预警：** `stock-validation.js`（R 存量开票额 <0 预警 / S 存量合同额 <0 阻断提交）
    - **业务逻辑：** `store.js`、`field-config.js`、`formula-engine.js`、`change-meta.js`、`sector-workflow.js`、`diff-utils.js`、`project-month-diff.js`、`import-merge.js`、`xlsx-importer.js`、`formatters.js`
    - **平台同步：** `platform-sync.js`（真实 API 对接）、`platform-sync-stub.js`（开发期 stub）
  - **会议提炼：** 完成 `产值报告线上化讨论_精修版.md`。
  - **自动化：** 工作日每日 9:00 自动检查并更新项目进度（Cron ID: `943d1900-...`）。
- **待办事项 (TODO)：**
  - [x] ~~确认角色字段级权限矩阵~~ → 已完成（§2.17）
  - [x] ~~7 项补充需求评估~~ → 已完成（§2.16）
  - [x] ~~数据库表结构设计~~ → 已完成
  - [x] ~~存量指标 R/S 预警与完成额校验~~ → 已实现（`stock-validation.js`）
  - [x] ~~平台数据同步框架~~ → 已实现（`platform-sync-stub.js`，真实 API 待对接）
  - [x] ~~项目详情 Drawer~~ → 已实现（`ProjectDetailDrawer.js`）
  - [ ] **平台数据同步 API 对接：** 替换 stub，对接真实中台/CRB/财务接口
  - [ ] **12 板块全量注册：** 在 `sectorRegistry` 中注册全部 SAS 执行单位
  - [ ] **时间窗配置后台：** 19/25/9 节点动态调整的前端交互形式
  - [ ] **WIP 报告流转机制：** 确定生成后的审批或通报流程
  - [ ] **成本法预留接口：** 评估是否需要预留
- **最新动态 (自动/手动更新)：**
  - _2026-05-21 10:50_: 前端新增 `ProjectDetailDrawer.js`（项目详情填报面板）、`project-drawer-layout.js`（布局模块）；填报页集成存量预警 `stock-validation.js`（R/S 列负值高亮、完成额超额阻断提交）；需求文档更新至 753 行（存量预警、平台同步、12 板块底栏等）；`ApprovalReviewSheet.js` 优化。
  - _2026-05-20 16:55_: 需求文档迭代至 v2.17（7 项补充需求 + 6×83 权限矩阵）；后端新增 `platform-sync.js` + `platform-sync-stub.js`（平台数据批量同步框架，含自动/手动同步、合并规则、底栏更新时间展示）；`store.js`、`server/index.js`、`server/db.js` 同步更新。
  - _2026-05-20 15:52_: 数据库表结构设计完成（`database-schema.sql` + `database-schema-design.md`，9 张表）。
  - _2026-05-20 10:57_: 前端新增 `AdminSettings.js`（管理设置页）、`Login.js`（登录页）、`AppLayout.js`（主布局）；审批页 `Approval.js`、`SystemAdminSectorDock.js`、`SystemAdminApprovalBoard.js` 实现。
  - _2026-05-20 09:00_: 需求文档更新至 v2.15+，阶段改为"原型开发与需求迭代"。
  - _2026-05-13 11:30_: 根据精修版会议记录完善需求文档（新增 §2 核心功能、§3 业务规则）。
  - _2026-05-13 10:40_: 初始化项目追踪表，完成需求文档梳理与技术栈定义。

---

## 🕰 自动化更新日志

_本文件由比比（Agent）在工作日每天上午 9:00 自动检查并更新最新进度。_

| 日期 | 更新时间 | 更新内容摘要 | 状态 |
| :--- | :--- | :--- | :--- |
| 2026-05-21 | 10:50 | 新增项目详情 Drawer、存量 R/S 预警模块、平台同步框架；需求文档 753 行 | ✅ |
| 2026-05-20 | 16:55 | 7 项补充需求 + 权限矩阵 + 平台同步 API 框架 | ✅ |
| 2026-05-20 | 15:52 | 数据库表结构设计完成（9 张表） | ✅ |
| 2026-05-20 | 10:57 | 管理设置页、登录页、审批看板、底栏组件等前端模块 | ✅ |
| 2026-05-20 | 09:00 | 需求文档更新至 v2.15+，阶段改为"原型开发与需求迭代" | ✅ |
| 2026-05-13 | 11:30 | 完善需求文档（双轨填报、锁定期、WIP 报表），技术栈定稿，定时任务配置完成 | ✅ |
| 2026-05-13 | 10:40 | 创建文件，初始化项目追踪表结构 | ✅ |
